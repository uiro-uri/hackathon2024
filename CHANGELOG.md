# Changelog

このプロジェクトの変更履歴。

バージョン規則:
- **機能追加・バランス調整** → マイナーを上げる（`+0.1`）
- **バグ修正** → パッチを上げる（`+0.0.1`）
- **メジャー** → 指定時のみ

バージョン番号の単一情報源は `godot/project.godot` の `application/config/version`。
変更をマージするたびに、この番号を上げて本ファイルに追記する。

## [0.2.0] - 2026-07-28
### Changed
- **オーバーウェイト**: 質量 `×1.5` → **`+0.75` の定数加算**。倍率は重ねるほど複利で伸び
  (1.5→2.25→3.38→5.06)、計測でも突出した最強札だった(Lv3を3枚で+75.8pt)。定数にすると
  1枚目の効きは `×1.5` と全く同じまま(1.5→2.25)、2枚目以降が線形になる(→3.0→3.75)。
  1枚目の魅力を落とさずに重ねがけの爆発だけを潰す。Lv5の3枚重ねは +60.2pt → +22.5pt。
- **ジャイアントグロース**: 直径 `×1.25` → **直径 `×1.1` + 質量 `×1.2` の複合**。半径だけを
  伸ばす札は、重ねるほど自然減衰と被弾面積が増えて実質デバフだった(3枚で-19.7pt、
  aim_spawn/Lv3)。「大きくなれば重くもなる」ぶんを質量に回し、罠札を解消した
  (同条件で +54.7pt)。
- 上の2枚に伴い、パーツの効果データを1組の `(stat, multiplier)` から `StatOp`
  (掛ける・足す・上限)の配列へ一般化。1枚で複数のステータスを触る札と、定数加算の札を
  同じ仕組みで書けるようにした。説明文は従来どおり数値から自動生成する。
- **バランスへの影響が大きい**: 罠札だったジャイアントグロース(COMMON=高頻度)が正の札に
  なったことで、`scripts/playtest.sh` のクリア率(intercept+greedy)が **14.7% → 93.7%**、
  ボス勝率が **86.3% → 99.3%** まで上がった。難度の再調整は別途。

## [0.1.0] - 2026-07-28
### Added
- フォントサイズの唯一の出所として `godot/scripts/core/font_scale.gd`(`FontScale`) を追加。
  役割(Display/Title/Subtitle/Heading/Body/Caption)ごとに横画面・縦画面の寸法を持ち、
  そこから Theme を組む。`.tscn` は `theme_type_variation` で役割名だけを持つ。

### Changed
- スマホ縦画面で文字が実質0.3倍にしか描かれず読めなかったのを解消。役割ごとに
  「小さい文字ほど強く上げる」倍率(Caption 2.0倍〜Display 1.33倍)で拡大し、
  固定サイズの器(報酬カード、取得済みパネル、ビルド表示HUD、隅のボタン)も同じ倍率で広げる。
- 報酬カードの並びを `HBoxContainer` から `GridContainer` にし、縦画面では1列に積む。
- Battle のメッセージだけ横画面の寸法が 36px から 32px(Subtitle) になった。
  7段階あった刻みを役割で説明できなかったための丸めで、横画面の変更はこの1箇所だけ。

### Fixed
- 検証の段階7(SP画面遷移)が、Titleのボタンを押せていないのに緑になっていた。押す位置を
  「画面中央+19px」という定数で出しており、Titleの中身が変わってずれていた。描かれた画素から
  ボタンを探す方式に変え、クリック前後で画面が実際に変わったかも判定基準に加えた。
### Changed
- GHOST（ゴースト）札をヒット&ラン化。すり抜け窓を「開始直後から N 秒」ではなく
  「**最初のプレイヤー対敵の衝突の直後**から N 秒」に移した。旧仕様は敵の攻撃だけでなく
  自分の初撃まで消すため、狙って撃つほど損をする自傷札になっていた。新仕様では狙った初撃は
  必ず通り、直後の報復ラッシュをすり抜けて離脱できる。
- 上に伴い `BattleResult` に `ghost_start`（窓が開いた時刻。開かなければ -1）を追加し、
  再生側のすり抜け表示（シマー・SE）を新しい窓に追従させた。
- ステータス表示の GHOST 行を「無敵時間 / Invincibility」から
  「すり抜け時間 / Pass-through」へ改称（実態と合わせた）。

## [0.0.0] - 2026-07-20
### Added
- バージョン管理を導入。単一情報源を `godot/project.godot` の `application/config/version`
  に置き、タイトル / ゲームクリア / ゲームオーバー画面の隅に `vX.Y.Z` を表示するようにした。
